var userInput = parseInt(prompt("Enter password length"));


if (!userInput) {
    alert("Not a number");
    userInput;
}
else {
    var nums = confirm('Do you want to use numbers');
    var specChar = confirm('Do you want to use special characters');
    var lowerCase = confirm('Do you want to use lower case');
    var upperCase = confirm('Do you want to use upper case');
}
